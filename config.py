# Configuration file for the CSV Data Merger

# Default file naming pattern settings
DEFAULT_FILE_PREFIX = "DATA"
DEFAULT_COLUMN_INDEX = 3  # Column D (0-based)
DEFAULT_START_ROW = 55    # Starting row (1-based)
DEFAULT_NUM_ROWS = 12     # Number of rows to extract

# Number of files to process (1 to NUM_FILES)
NUM_FILES = 6

# Output file settings
OUTPUT_FILE_PREFIX = "merged_data"

# GUI settings
WINDOW_TITLE = "Select folder containing CSV files"
